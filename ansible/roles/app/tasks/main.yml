---
# tasks file for roles/database
- name: Run Database
  docker_container:
    name: db-host
    image: schrk06/tp-devops-database:latest
    networks:
      - name: app-network
    restart_policy: always
    env:
      POSTGRES_DB: db
      POSTGRES_USER: usr
      POSTGRES_PASSWORD: pwd