---
# tasks file for roles/app
- name: Run Backend App
  docker_container:
    name: backend
    image: schrk06/tp-devops-simple-api:latest
    networks:
      - name: app-network
    restart_policy: always
    env:
      POSTGRES_DB: db
      POSTGRES_USER: usr
      POSTGRES_PASSWORD: pwd